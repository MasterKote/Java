<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ПР06 Работа с объектами</title>
</head>
<body>
    <h1>ПР06 Работа с объектами</h1>
    <p>Результат выполнения кода будет выведен в консоль браузера (F12).</p>

    <script>
        // --- 1. Создание Объектов ---

        // 1.1 Объект 1: Полное описание структуры
        let grinning_face_with_smiling_eyes = {
            emoji: "😄",
            name: "grinning face with smiling eyes",
            shortname: ":grinning_face_with_smiling_eyes:",
            unicode: "1F601",
            html: "&#128513;",
            category: "Smileys & Emotion (face-smiling)",
            order: 1 
        };

        // 1.2 Объект 2: Пустой объект с последующим заполнением
        let rocket = {};

        rocket.emoji = "🚀";
        rocket.name = "rocket";
        rocket.shortname = ":rocket:";
        rocket.unicode = "1F680";
        rocket.html = "&#128608;";
        rocket.category = "Travel & Places (transport-air)";
        rocket.order = 2;

        // 1.3 Объект 3: Функция-конструктор
        function Emoji(emoji, name, shortname, unicode, html, category, order) {
            this.emoji = emoji;
            this.name = name;
            this.shortname = shortname;
            this.unicode = unicode;
            this.html = html;
            this.category = category;
            this.order = order;
        }

        let folded_hands = new Emoji(
            "🙏",
            "folded hands",
            ":folded_hands:",
            "1F64F",
            "&#128591;",
            "People & Body (hand-fingers-closed)",
            3
        );

        // --- 2. Работа со Свойствами Объекта ---

        // 2.1. Удалить все свойства из первого объекта, кроме name и category.
        delete grinning_face_with_smiling_eyes.emoji;
        delete grinning_face_with_smiling_eyes.shortname;
        delete grinning_face_with_smiling_eyes.unicode;
        delete grinning_face_with_smiling_eyes.html;
        delete grinning_face_with_smiling_eyes.order;

        // 2.2. Во второй объект добавить свойство is my cool со значением true
        rocket["is my cool"] = true;

        // --- 3. Клонирование (Объединение) ---

        // Оператор расширения (...) для поверхностного объединения/клонирования
        let combinedEmoji = {
            ...grinning_face_with_smiling_eyes,
            ...rocket,
            ...folded_hands
        };

        // Вывод в консоль
        console.log("--- Исходный объект 1 после изменений (только name, category) ---");
        console.log(grinning_face_with_smiling_eyes);

        console.log("--- Исходный объект 2 после изменений (с 'is my cool') ---");
        console.log(rocket);
        
        console.log("--- Исходный объект 3 (функция-конструктор) ---");
        console.log(folded_hands);

        console.log("--- Итоговый объединенный объект (клонирование и слияние) ---");
        console.log(combinedEmoji);
        console.log("-------------------------------------------------------------");
    </script>
</body>
</html>
